export const COLLECTION = {
  reading: "reading",
  speaking: "speaking",
  grammar: "grammar",
  listening: "listening",
};

export enum LessonType {
  GRAMMAR = 'GRAMMAR',
  LISTENING = 'LISTENING',
  READING = 'READING',
}

export enum UserHistoryType {
  GRAMMAR = 'GRAMMAR',
  LISTENING = 'LISTENING',
  READING = 'READING',
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
  EXAM = 'EXAM',
}
